<!DOCTYPE html>
<html lang="en">

{{>head fontless=true}}

<body class="main">

	<!-- layout -->
	<div class="root">		

		<div class="logged-block">
			<div class="logged-col col-basis col-55 logged-flex">
				<div class="logged-box">
					<div class="logged-logo">
						<a href="index.html">
							<img src="./asset/images/logo-pion-clinician.png" alt="pion-clinician">
						</a>
					</div>
					<div class="logged-title">Lupa kata sandi kamu?</div>
					<div class="logged-lead">Segera atur ulang kata sandi sebelum akun kamu hilang.</div>
				</div>
				<div class="logged-bg">
					<img src="./asset/images/index/forget.svg" alt="Reset Password">
				</div>
			</div>
			<div class="logged-col col-basis col-45 logged-flex">
				<div class="logged-card card">
					<div class="form-main form-login">
						<h2 class="title">Atur ulang kata sandi</h2>
						<div class="form-hint">
							<p>Masukkan email atau nomor telepon kamu untuk melanjutkan. Kami akan kirimin kamu tautan dengan instruksi.</p>
						</div>
						<form action="forget-help-v2.html">
							{{>input type="text" label="Email / Phone Number" name="email" placeholder="Email / Phone Number" txterror=txterrorEmail txtVal=txtValEmail nolabel="true"}}
							<hr>
							{{#if txterrorSystem}}
							<div class="form-error text-center">{{txterrorSystem}}</div>
							{{/if}}
							{{>button name="forget" type="submit" label="Reset Password" class="-disabled" disabled="true"}}
							<div class="block-login">
								<p><a href="register-v2.html">Daftar Pion</a> atau <a href="login-v2.html">Masuk</a></p>
								<p><a href="index.html">Kembali ke halaman utama</a></p>
							</div>
						</form>
					</div>
				</div>
			</div>
		</div>

	</div>

{{>js}}<script>
	// script untuk keperluan dummy saja
	let ss = document.querySelectorAll("input:not([type=submit])");
	let fr = document.querySelector("input[type=submit]");
	if(!!ss) {
		for(let i=0;i<ss.length;i++) {
			ss[i].addEventListener('input', cekInput);
			ss[i].addEventListener('propertychange', cekInput); 
		}
	}
	function cekInput(e) {
		let va = e.target.value;
		for(let i=0;i<ss.length;i++) {
			console.log(ss[i].value);
			if(ss[i].value!='') {
				fr.classList.add("-primary");
				fr.classList.remove("-disabled");
				fr.removeAttribute("disabled");
			} else {
				fr.classList.add("-disabled");
				fr.setAttribute("disabled","disabled");
			}
		}
	} 
</script>
</body>
</html>
